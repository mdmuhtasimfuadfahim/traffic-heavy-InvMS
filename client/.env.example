# Local dev default (matches server/.env.example's PORT).
# Production (set this on the Vercel project instead, not in a committed file):
#   VITE_API_URL=https://invms-server.onrender.com
VITE_API_URL=http://localhost:8977
